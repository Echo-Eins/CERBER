# === CEBCM Project Dependencies ===
# === Optimized for NVIDIA DGX Spark (GB10 Grace Blackwell, ARM64/aarch64, CUDA 12.8+) ===
#
# INSTALLATION ORDER MATTERS — follow steps 1-5 in sequence:
#
# STEP 1: PyTorch with Blackwell (SM_100) + CUDA 12.8 + ARM64
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/cu128
#
# STEP 2: Flash Attention 3 (native Blackwell/Hopper kernels, ~2-3x faster attention)
#   MAX_JOBS=8 pip install flash-attn --no-build-isolation
#   # Requires CUDA toolkit headers: export CUDA_HOME=/usr/local/cuda
#
# STEP 3: NVIDIA Transformer Engine (FP8 training on Blackwell, ~2x throughput)
#   pip install transformer-engine[pytorch]
#   # Enables torch.float8_e4m3fn precision on Blackwell
#
# STEP 4: xformers (memory-efficient attention, fused kernels)
#   pip install xformers --index-url https://download.pytorch.org/whl/cu128
#
# STEP 5: SONAR / fairseq2
#   pip install fairseq2 \
#       --extra-index-url https://fair.pkg.atmeta.com/fairseq2/whl/pt2.7.0/cu128
#
#   NOTE: If ARM64 (aarch64) wheels are unavailable at the index above, build from source:
#     git clone https://github.com/facebookresearch/fairseq2 && cd fairseq2
#     CUDAARCHS="100" pip install --no-build-isolation -e .
#
# STEP 6: faiss-gpu (ARM64 note: pip wheel may not exist; use conda or build from source)
#   conda install -c pytorch -c nvidia faiss-gpu=1.9.0
#   # OR from source: https://github.com/facebookresearch/faiss/blob/main/INSTALL.md

sonar-space>=0.5.0

# --- Datasets & NLP ---
datasets>=2.14.0
transformers>=4.46.0
sentence-transformers>=3.0.0

# --- Retrieval ---
# faiss-gpu preferred; fallback to faiss-cpu if GPU wheels unavailable for ARM64
# faiss-cpu>=1.7.4

# --- Metrics ---
rouge-score>=0.1.2
nltk>=3.8.0

# --- Utilities ---
tqdm>=4.65.0
wandb>=0.15.0
numpy>=1.26.0
pyyaml>=6.0

# --- GPU Monitoring ---
nvidia-ml-py>=12.0.0       # pynvml: GPU utilization, memory, temp

# === GUI & Visualization (for cerber_gui) ===
gradio>=4.0.0
plotly>=5.18.0
pandas>=2.0.0
watchdog>=3.0.0
